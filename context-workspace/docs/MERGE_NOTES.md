# context-workspace — merge record

This module existed as two independently-evolved fix passes on the same original 12-file delivery. This merge combines both. See archive comments for the full rationale.
